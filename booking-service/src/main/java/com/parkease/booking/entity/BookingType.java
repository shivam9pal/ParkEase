package com.parkease.booking.entity;

public enum BookingType {
    PRE_BOOKING,    // Advance reservation — spot reserved before arrival
    WALK_IN         // Immediate booking on arrival — direct check-in
}